/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AmpmPipe } from './ampm.pipe';

describe('Pipe: Ampme', () => {
  it('create an instance', () => {
    let pipe = new AmpmPipe();
    expect(pipe).toBeTruthy();
  });
});
